# copy and adapt this server template

server {
  root /usr/share/nginx/www;
	index index.html index.htm;

        listen 8000;

	# Make site accessible from http://localhost/
	server_name localhost;

}
